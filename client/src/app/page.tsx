import { Container } from "@mui/material";
import Navbar from "../components/layout/Navbar";

const Home = () => {
  return (
    <main>
      <section style={{ padding: "24px 64px", backgroundColor: "black" }}>
        <Navbar />
      </section>
    </main>
  );
};

export default Home;
